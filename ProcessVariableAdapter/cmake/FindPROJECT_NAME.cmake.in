#
# cmake module for finding @PROJECT_NAME@
#
# returns:
#   @PROJECT_NAME@_FOUND        : true or false, depending on whether
#                                   the package was found
#   @PROJECT_NAME@_VERSION      : the package version
#   @PROJECT_NAME@_INCLUDE_DIRS : path to the include directory
#   @PROJECT_NAME@_LIBRARY_DIRS : path to the library directory
#   @PROJECT_NAME@_LIBRARIES    : the provided libraries
#
# @author Martin Killenberg, DESY
#

SET(@PROJECT_NAME@_FOUND 0)

#FIXME: the search path for the device config has to be extended/generalised/improved
FIND_PATH(@PROJECT_NAME@_DIR
    @PROJECT_NAME@Config.cmake
    ${CMAKE_CURRENT_LIST_DIR}
    )

#Once we have found the config our job is done. Just load the config which provides the required 
#varaibles.
include(${@PROJECT_NAME@_DIR}/@PROJECT_NAME@Config.cmake)

#use a macro provided by CMake to check if all the listed arguments are valid
#and set @PROJECT_NAME@_FOUND accordingly
include(FindPackageHandleStandardArgs)
FIND_PACKAGE_HANDLE_STANDARD_ARGS(@PROJECT_NAME@ 
        REQUIRED_VARS @PROJECT_NAME@_LIBRARIES @PROJECT_NAME@_INCLUDE_DIRS
	VERSION_VAR @PROJECT_NAME@_VERSION )

